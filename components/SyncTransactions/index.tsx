import React from "react";

const SyncTransaction = () => {
	// fetch data from moralis with useTransactions

	// all sync transactions should be done here

	/**
	 *
	 *
	 * fetch tx from etherscan --> show the list of all tnxs --> user picks which txn to be added in log
	 *
	 *
	 */

	return <div>as🕊 </div>;
};

export default SyncTransaction;
